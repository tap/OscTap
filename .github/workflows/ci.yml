name: CI

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  build-test:
    name: ${{ matrix.os }} / ${{ matrix.cxx || 'msvc' }} / C++${{ matrix.std }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest,  cc: gcc,    cxx: g++,     std: 17 }
          - { os: ubuntu-latest,  cc: gcc,    cxx: g++,     std: 20 }
          - { os: ubuntu-latest,  cc: clang,  cxx: clang++, std: 17 }
          - { os: ubuntu-latest,  cc: clang,  cxx: clang++, std: 20 }
          - { os: macos-latest,   cc: clang,  cxx: clang++, std: 17 }
          - { os: macos-latest,   cc: clang,  cxx: clang++, std: 20 }
          - { os: windows-latest, std: 17 }
          - { os: windows-latest, std: 20 }
    steps:
      - uses: actions/checkout@v4

      - name: Configure
        run: cmake -S . -B build -DOSCPACK_BUILD_EXAMPLES=ON -DOSCTAP_WARNINGS_AS_ERRORS=ON -DCMAKE_CXX_STANDARD=${{ matrix.std }}
        env:
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}

      - name: Build
        run: cmake --build build --config Release

      - name: Test
        run: ctest --test-dir build -C Release --output-on-failure

  sanitizers:
    name: ASan + UBSan (unit tests)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure with sanitizers
        run: >
          cmake -S . -B build -DOSCPACK_BUILD_EXAMPLES=ON -DOSCTAP_WARNINGS_AS_ERRORS=ON
          -DCMAKE_BUILD_TYPE=Debug
          -DCMAKE_CXX_FLAGS="-fsanitize=address,undefined -fno-sanitize-recover=all -g"
          -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address,undefined"
        env:
          CC: gcc
          CXX: g++

      - name: Build
        run: cmake --build build

      - name: Run unit tests under sanitizers
        # The test harness intentionally leaks its aligned scratch buffers
        # (NewMessageBuffer), so leak detection is disabled here; ASan still
        # checks for overflows/use-after-free and UBSan for undefined behaviour.
        run: ASAN_OPTIONS=detect_leaks=0 ctest --test-dir build --output-on-failure

  fuzz-smoke:
    name: Fuzzer smoke test (standalone + ASan)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure standalone fuzzers
        run: cmake -S . -B build-fuzz -DOSCPACK_BUILD_EXAMPLES=OFF -DOSCTAP_FUZZER_STANDALONE=ON

      - name: Build fuzzers
        run: cmake --build build-fuzz --target fuzz_parse fuzz_deframe

      - name: Replay corpus and run mutation loop (parser)
        run: ./build-fuzz/fuzz_parse fuzz/corpus/*

      - name: Replay corpus and run mutation loop (TCP deframer)
        run: ./build-fuzz/fuzz_deframe fuzz/corpus_deframe/*

  rtsan:
    name: RTSan + function-effects (realtime hot path)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # RealtimeSanitizer (-fsanitize=realtime) and the function-effect analysis
      # (-Wfunction-effects, for [[clang::nonblocking]]) require Clang >= 20.
      - name: Install Clang 20 + RTSan runtime
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-20 libclang-rt-20-dev

      - name: Configure (RTSan + function-effects)
        run: cmake -S . -B build-rtsan -DOSCPACK_BUILD_EXAMPLES=ON -DOSCTAP_RTSAN=ON
        env:
          CC: clang-20
          CXX: clang++-20

      - name: Build
        run: cmake --build build-rtsan

      # The realtime test runs its read/dispatch hot path inside a
      # [[clang::nonblocking]] region; RTSan aborts on any realtime-unsafe call.
      - name: Run unit + realtime tests under RTSan
        run: ctest --test-dir build-rtsan --output-on-failure

  freestanding:
    name: Freestanding profile (${{ matrix.cxx }}, -fno-exceptions -fno-rtti)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - { cc: gcc,   cxx: g++ }
          - { cc: clang, cxx: clang++ }
    steps:
      - uses: actions/checkout@v4

      # Builds only the embedded-profile smoke test: the parse/serialize core
      # with C++ exceptions and RTTI disabled and the hosted-only facilities
      # dropped (-DOSCTAP_FREESTANDING). This is the standing guard that the
      # core stays buildable for embedded targets (e.g. a Raspberry Pi Pico 2W;
      # see docs/EMBEDDED_PICO2W.md). The OSCTAP_THROW seam routes validation
      # failures to a fatal handler when exceptions are off.
      - name: Configure (freestanding profile)
        run: >
          cmake -S . -B build-freestanding -DOSCPACK_BUILD_EXAMPLES=OFF
          -DOSCTAP_FREESTANDING=ON -DOSCTAP_WARNINGS_AS_ERRORS=ON
        env:
          CC: ${{ matrix.cc }}
          CXX: ${{ matrix.cxx }}

      - name: Build
        run: cmake --build build-freestanding --target OscFreestandingTest

      - name: Run freestanding smoke test
        run: ctest --test-dir build-freestanding -R OscFreestandingTest --output-on-failure

  aarch64-qemu:
    name: aarch64 cross-build + qemu (Pi-5-class / big-endian-agnostic)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Cross-compile for 64-bit Arm (the Raspberry Pi 5 ISA) and run the suite
      # under qemu-user, so the aarch64 build is proven green on every push. This
      # also exercises the endian-agnostic (de)serialization on a non-x86 target.
      - name: Install aarch64 toolchain + qemu-user
        run: |
          sudo apt-get update
          sudo apt-get install -y g++-aarch64-linux-gnu qemu-user-static

      - name: Configure (aarch64 cross; qemu as the test emulator)
        run: >
          cmake -S . -B build-aarch64 -DOSCPACK_BUILD_EXAMPLES=ON
          -DOSCTAP_WARNINGS_AS_ERRORS=ON
          -DCMAKE_SYSTEM_NAME=Linux -DCMAKE_SYSTEM_PROCESSOR=aarch64
          -DCMAKE_C_COMPILER=aarch64-linux-gnu-gcc
          -DCMAKE_CXX_COMPILER=aarch64-linux-gnu-g++
          "-DCMAKE_CROSSCOMPILING_EMULATOR=qemu-aarch64-static;-L;/usr/aarch64-linux-gnu"

      - name: Build
        run: cmake --build build-aarch64

      # OscConcurrencyTest is excluded: it uses real threads + loopback sockets,
      # which are flaky under qemu-user emulation. Native TSan/POSIX legs already
      # cover it; here we vet the parse/serialize suite on the aarch64 ISA.
      - name: Run suite under qemu
        run: ctest --test-dir build-aarch64 -E OscConcurrencyTest --output-on-failure

  win32-wine:
    name: win32 sockets runtime (MinGW + Wine)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # The win32 socket backends are otherwise only compile-checked (the MSVC
      # legs + the MinGW smoke). Here we cross-compile the real socket loopback
      # tests for win32 and run them under Wine, so the win32 UDP + TCP backends
      # get actual runtime coverage on every push.
      - name: Install MinGW-w64 + Wine
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y g++-mingw-w64-x86-64 wine64 wine

      - name: Cross-compile the socket loopback tests for win32
        run: |
          for t in OscUdpTest OscTcpTest; do
            x86_64-w64-mingw32-g++ -std=c++17 -O1 -static -I . -I osctap \
              tests/$t.cpp -o $t.exe -lws2_32 -lwinmm
          done

      - name: Run under Wine
        env:
          WINEPREFIX: /tmp/wineprefix
          WINEDEBUG: "-all"
          WINEDLLOVERRIDES: "mscoree,mshtml="   # skip the mono/gecko install prompts
        run: |
          wineboot -i || true
          wineserver -w || true
          for t in OscUdpTest OscTcpTest; do
            echo "=== $t (win32, under Wine) ==="
            wine ./$t.exe
          done

  tsan:
    name: ThreadSanitizer (receive loop concurrency)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # ThreadSanitizer ships with GCC (libtsan), so no extra runtime install.
      - name: Configure (TSan)
        run: cmake -S . -B build-tsan -DOSCPACK_BUILD_EXAMPLES=ON -DOSCTAP_TSAN=ON
        env:
          CC: gcc
          CXX: g++

      - name: Build
        run: cmake --build build-tsan --target OscConcurrencyTest

      # Run Run() vs AsynchronousBreak() under TSan; halt_on_error makes any data
      # race fail the job rather than just print a warning.
      - name: Run concurrency test under TSan
        run: TSAN_OPTIONS=halt_on_error=1 ctest --test-dir build-tsan -R OscConcurrencyTest --output-on-failure
